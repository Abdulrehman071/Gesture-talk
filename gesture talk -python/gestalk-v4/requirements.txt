# GestaTalk v4.1 — Python Dependencies
# Install with: pip install -r requirements.txt

# Core CV & hand tracking
opencv-python>=4.8.0
mediapipe>=0.10.0

# TTS — Windows SAPI (most reliable, no pyttsx3 needed)
pywin32>=306

# Utilities
numpy>=1.24.0
